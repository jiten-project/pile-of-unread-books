name: Keep Supabase Alive
on:
  schedule:
    - cron: '0 0 * * *'
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - run: curl -X GET "${{ secrets.SUPABASE_URL }}/rest/v1/books?limit=1" -H "apikey: ${{ secrets.SUPABASE_ANON_KEY }}"
